package io.openems.edge.bridge.mqtt.manager;

//TODO: GET THE COMPONENTS AND CHECK IF THEY'RE CONFIGURED VIA JSON --> IF CONFIGURED VIA JSON UPDATE THEM; IF CONFIG CHANGED --> RELOAD
public class ComponentManagerImpl {
}
